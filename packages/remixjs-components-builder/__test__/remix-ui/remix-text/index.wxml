<import src="../remix-worker.wxml" /> 
<text child="{{child}}" innerText="{{innerText}}" id="{{uuid}}" style="{{styles}}" class="{{className}}" selectable="{{selectable}}" space="{{space}}" decode="{{decode}}"><block wx:if="{{innerText}}">{{innerText}}</block><block wx:elif="{{child}}"><template is="remix-worker" data="{{ element: child }}" /></block>
</text>
